[project]
name = "quantlib-rag"
version = "0.1.0"
description = "QuantLib Retrieval-Augmented Generation Engine"
requires-python = ">=3.10"

[tool.setuptools]
package-dir = {"" = "src"}

[tool.setuptools.packages.find]
where = ["src"]